<ul class="gallery">
{% for image in site.static_files %}

    {% if image.path contains 'picture-clouds' %}
    <li>
      <a href="{{ image.link }}">
        <img src="{{ site.baseurl }}{{ image.path }}" />
      </a>
    </li>

    {% endif %}
{% endfor %}
</ul>
